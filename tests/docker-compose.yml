version: '3.8'
services:
  redis:
    image: redis:latest

  app:
    build:
      dockerfile: ./dev/Dockerfile
      context: ..
    command: npm start -- --config.file /etc/app/config.yml
    links:
      - redis
    volumes:
      - /home/bagas/devops/projects/infra-related/event-processor-autoscaler/dev/etc/app/config.yml:/etc/app/config.yml
